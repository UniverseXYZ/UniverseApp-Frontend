import { FlexProps } from '@chakra-ui/react';

export const StatisticsContainerStyle: FlexProps = {
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'column',
    bgColor: 'white',
    padding: '30px 0',
}
